"""Strategic reference material the coach can cite. (Phase 3)

Intended shape: a small, curated corpus of named concepts — board-texture
classes, standard preflop charts, common leak patterns — that the coaching
layer retrieves from and cites by name, so advice can point at a source rather
than assert from nowhere. `examples/knowledge/` is reserved for that seed
material and is empty until the corpus is written.

Deliberately empty in Phase 2: retrieval design depends on how the coaching
prompt actually performs, and guessing at it now would bake in the wrong shape.
"""

__all__: list[str] = []
